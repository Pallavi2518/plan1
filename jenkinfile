pipeline {
    agent any

    stages {
        stage('Build') {
            steps {

                    echo "compiling"
                    sh "javac Plant.java"
                    echo "executing"
                    sh "java Plant"
    
}
        }
    }
}
